# This is our bundler Gemfile to help make installation of ruby gem dependencies easier
source 'https://rubygems.org'

# Everybody likes color, Colorize makes it easy :p
gem 'colorize'

# Ruby Readline support is needed for pseudo shell and ease of use for UI
gem 'rb-readline'

# We need SNMP Gem to leverage for its huge MiBs support
gem 'snmp'

# Text-Table makes some of the formatting of output easier
gem 'text-table'
